# Frontend / Vercel environment variables
# Copy to .env and fill in values

# Required: Backend VM URL for API proxying
CLOUD_RUN_URL=http://34.10.240.173:8080

# Optional: GCS service account key (base64-encoded JSON) for signed upload URLs
GCS_SERVICE_ACCOUNT_KEY=

# Optional: Claude API key for direct /api/analyze proxy
ANTHROPIC_API_KEY=
