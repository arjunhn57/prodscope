# Dev Server environment variables
# Copy to .env and fill in values

# Optional: Override backend URL for API proxying (default: http://34.10.240.173:8080)
# BACKEND_BASE=http://localhost:8080

# Optional: Dev server port (default: 3000)
# PORT=3000
